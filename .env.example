# Environment Configuration for WPPOOL Selenium Tests

# Copy this file to .env and replace the placeholder values with your actual credentials

# WordPress Site Configuration
WP_URL=https://your-wordpress-site.com/wp-admin
WP_USER=your_wordpress_username
WP_PASS=your_wordpress_password

# Google Sheets Configuration (for FlexTable plugin tests)
GOOGLE_SHEET_LINK=https://docs.google.com/spreadsheets/d/your_sheet_id/edit

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace all placeholder values with your actual data
# 3. Never commit .env to version control (it's already in .gitignore)